//read two 3x3 integer matrices, compute their sum into a third matrix, 
// and print all three matrices clearly labeled

#include <stdio.h>

int main() {


int A[3][3];
int B[3][3];
int C[3][3];

int a, b;

printf("enter elements for matrix A: ");{
for (int i = 0; i < 3; i++) {
printf("A[%d][%d]: ", a,b);
scanf("%d", &A[a][b]);
}
}

printf("enter elements for matrix B: ");{
for (int i = 0; i < 3; i++) {
printf("B[%d][%d]: ", a,b);
scanf("%d", &B[a][b]);
}
}

// ??
  
return 0;
}
